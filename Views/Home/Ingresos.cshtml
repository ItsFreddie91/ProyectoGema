@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@model List<Proyecto.Models.Ingreso>

<div class="rounded shadow bg-light p-4 mt-3 mb-2">
    <div class="mt-2 mb-2 text-center">
        <h2>Lista de Ingresos</h2>
    </div>
    <div class="py-3">
        <table class="table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Descripción</th>
                    <th>Monto</th>
                    <th>Fecha</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>@item.Descripcion</td>
                        <td>@item.Monto</td>
                        <td>@item.Fecha</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>